server:
  port: 8888

spring:
  application:
    name: config-server

cloud:
   config:
     server:
       git:
         uri: https://github.com/UdyogSet/config-repo.git
         clone-on-start: true
         default-label: main  # branch name
         search-paths:
           - config
